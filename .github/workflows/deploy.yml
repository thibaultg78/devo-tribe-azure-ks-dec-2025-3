name: Build and Deploy to Azure

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Azure login
        uses: azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}"
            }
      
      - name: Login to Azure Container Registry
        run: |
          az acr login --name acrdevoteamdemo3
      
      - name: Build and push image to ACR
        run: |
          az acr build \
            --registry acrdevoteamdemo3 \
            --image devo-tribe-azure-ks-dec-2025-3:${{ github.sha }} \
            --image devo-tribe-azure-ks-dec-2025-3:latest \
            --file Dockerfile \
            .
      
      - name: Deploy to Azure Container Instances
        run: |
          az container create \
            --resource-group RG-Thibault-Gibard \
            --name devo-tribe-azure-ks-dec-2025-3 \
            --image acrdevoteamdemo3.azurecr.io/devo-tribe-azure-ks-dec-2025-3:latest \
            --registry-login-server acrdevoteamdemo3.azurecr.io \
            --registry-username ${{ secrets.AZURE_CLIENT_ID }} \
            --registry-password ${{ secrets.AZURE_CLIENT_SECRET }} \
            --dns-name-label devo-tribe-azure-ks-dec-2025-3 \
            --ports 80 \
            --os-type Linux \
            --cpu 1 \
            --memory 1 \
            --environment-variables PORT=80
